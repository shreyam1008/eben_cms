import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App";
import "./index.scss";

// new react version change on render methind to older version.
const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(<App />);
